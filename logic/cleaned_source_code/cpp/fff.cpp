int partc(string S , int k){string  "a";string  "";int 0;while( low.length() < S.length()) {low;for(int 0;i<high.length();i++) {low.append(fundamental[(int)(high[i]-'a')]);power+=1;//  high;// "";string  high + low + high;string  high + high;int lps[S.length()];int  KMPdo(S, stringABA, lps);if(first!=-1){int  first + S.length();if(last < low.length()){return power;else if(last < low.length() + high.length()) {return power+1;else return power+2;int  KMPdo(S, stringAA, lps);if(second != -1){return power+3;return -1;