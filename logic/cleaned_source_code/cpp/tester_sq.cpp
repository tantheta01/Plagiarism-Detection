#include <bits/stdc++.h>#include <chrono>#include"./190050122.h"using namespace std;typedef long long ll;#define pb push_backconst ll  1e18;int pro(int n, int aray[])vector<int> v;int visit[n] = {0},  0;for(int 0;i<n;i++)if(visit[i])continue;1;int  aray[i];while(curr != i)c++;aray[curr];v.pb(c);int  1;for(auto x: v)ans/__gcd(ans, x)*x;return ans;int main()srand(time(0));int  100;int aray[n];for(ll 0;i<n;i++)aray[i] = i;int  100000;int  0,  0,  0;int  0;while(t--)ll  rand()%(INF);random_shuffle(aray, aray+n);permutation  permutation(n, aray);int  pro(n, aray);auto  std::chrono::high_resolution_clock::now();permutation  p^2;q.square_root();p^2;auto  std::chrono::high_resolution_clock::now();auto  std::chrono::duration_cast<std::chrono::microseconds>(stop - start);total += duration.count();int * q.to_array(), * p.to_array();for(int 0;i<n;i++)if(a[i] != arr[i])c++;break;cout<<"Number of wrong answers: "<<c<<"\n";cout<<"Execution time (in microseconds): "<<total<<"\n";return 0;